# CLAUDE.md

Repository-specific guidance for Kinfolk. See [CONTRIBUTING.md](CONTRIBUTING.md) for the public development workflow.

## Project

Kinfolk is a family tree maker where relatives each keep their own tree and then link them, so the combined graph shows how several families actually join up. It uses GitHub OAuth sign-in.

The distinguishing feature is the combined view: two people who have never met can both record the same grandparent, and Kinfolk merges those records without either side losing their own data.

It is also a contact graph, not only a pedigree. Anyone can be a node -- a friend, a colleague, a neighbour -- and any two nodes can carry a named relation with their own contact details attached.

## Stack

- **Language**: TypeScript 7
- **Framework**: Next.js 16.3 (App Router, Turbopack)
- **Database**: Neon Postgres via Drizzle ORM
- **Auth**: Auth.js v5 (`next-auth@5` beta), GitHub provider only, database sessions
- **UI**: Tailwind 4 with one shared archival field-desk system, React Flow (`@xyflow/react`) canvas, ELK (`elkjs`) layout, Motion, lucide-react. `BrandFrame` shares public framing across Next and Vite without sharing runtime actions.
- **Package manager**: pnpm
- **Deploy target**: Vite SPA on GitHub Pages, with the API and server-rendered fallback on Vercel + Neon. See [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md).

## Deploy state, as of 2026-08-08

Two deployments are live:

- The Vite SPA is served from <https://sagargupta.online/kinfolk/> by GitHub Pages.
  Pages uploads only `frontend/dist`; source maps remain disabled.
- The API, OAuth exchange, writes, and server-rendered fallback are served from
  <https://kinfolk-neon.vercel.app>.
- The Next app cannot be statically exported because Auth.js, route handlers, and server
  actions require a server. The Vite SPA is the static front end and stores its bearer
  token in `sessionStorage`; the Next fallback uses an httpOnly cookie.
- Production uses one deployment environment. `scripts/vercel-ignore-build.sh` skips
  documentation-only Vercel builds, while unknown paths build by default.
- `AUTH_TRUST_HOST=true` is required behind Vercel's proxy. `AUTH_URL` must be the exact
  deployment origin, and the GitHub OAuth callback must match it exactly.
- Vercel uses the Neon pooler URL. Local Drizzle schema commands use a direct URL. Ordered
  production schema changes go through committed migrations, never `db:push`.
- Unexpected API failures log only a non-sensitive stage or operation; browser responses remain
  generic, and error objects with SQL, bound values, or provider payloads are not persisted.
- Set secrets only through the deployment provider's trusted secret interface. Never put
  credentials in automation transcripts, commands, logs, issues, or repository files.

## Run

```
pnpm install
pnpm dev --port 3007
pnpm build
```

## Test

```
pnpm lint
pnpm typecheck
pnpm --dir frontend typecheck
pnpm build
pnpm --dir frontend build
pnpm audit --audit-level=low
pnpm audit --prod --audit-level=low
git diff --check
```

The unit suite and live smoke scripts were removed in the 0.2.0 rework. For canvas
changes, also verify the documented node/edge counts, geometry, mobile layout, and a
clean browser console in both frontends.

## Database

The production database runs Postgres 17. Schema changes are committed as ordered Drizzle migrations and applied by the deployment workflow.

**Migration `0003_fearless_mongu.sql` is applied in production.** [Deploy run 31256902754](https://github.com/Sagargupta16/kinfolk/actions/runs/31256902754) verified 3/4 committed migrations before `drizzle-kit migrate`, then complete-checked 4/4 and passed every public endpoint probe. The file is tracked schema history—not a database dump—and contains one `CREATE UNIQUE INDEX` for `people_claimed_user_idx`, with no genealogy rows or credentials.

Release 0.3.1 adds schema-only migration `0004_dusty_karen_page.sql`: one atomic creation-budget counter table and its cascading tree foreign key. It contains no family records or credentials. The runtime temporarily falls back to the previous timestamp count if application promotion wins the race with migration deployment.

## Entry points

- [app/page.tsx](app/page.tsx) -- landing page
- [app/tree/page.tsx](app/tree/page.tsx) -- the tree. One page, two data sources; resolves session first, demo cookie second.
- [app/signin/page.tsx](app/signin/page.tsx) -- GitHub sign-in, with the demo offered just as prominently
- [app/demo/route.ts](app/demo/route.ts) -- sets the demo cookie and redirects to `/tree`. A route handler, not a page: a page cannot set a cookie during render.
- [app/api/auth/[...nextauth]/route.ts](app/api/auth/%5B...nextauth%5D/route.ts) -- Auth.js handler
- [auth.ts](auth.ts) -- Auth.js config plus `sessionOrNull()`, which pages use instead of `auth()`

## Key files

- [lib/db/schema.ts](lib/db/schema.ts) -- the brain. Read this before touching any data logic.
- [lib/tree/relations.ts](lib/tree/relations.ts) -- `RELATION_KINDS`, the single source of truth for what each relation means, its inverse, and whether it is symmetric. Insert, display, and canvas all read it.
- [lib/tree/graph.ts](lib/tree/graph.ts) -- fusion (union-find) plus React Flow projection. Pure, no browser or DB deps.
- [lib/tree/layout.ts](lib/tree/layout.ts) -- ELK layered layout followed by household alignment: partner-connected people share one packed row, parent households rank above child households, and fixed node dimensions remain authoritative
- [lib/tree/siblings.ts](lib/tree/siblings.ts) -- the horizontal sibling bar and its lane assignment, computed AFTER layout from positions
- [lib/tree/kinship.ts](lib/tree/kinship.ts) -- `kinshipMap()`, what every person is to the viewer ("grandmother", "second cousin", "friend"). The card's second line.
- [lib/tree/view.ts](lib/tree/view.ts) -- `TreeView`, the one shape the UI renders. Demo and database both produce it.
- [lib/tree/load.ts](lib/tree/load.ts) -- the database path to a `TreeView`; [lib/tree/demo.ts](lib/tree/demo.ts) is the sample-data path
- [lib/tree/visibility.ts](lib/tree/visibility.ts) -- server-side contact filtering. Nothing else may decide what a viewer receives.
- [lib/tree/authz.ts](lib/tree/authz.ts) -- who may WRITE. Separate from the read path on purpose; every mutation starts here.
- [lib/tree/edit-actions.ts](lib/tree/edit-actions.ts) -- every write the editor can perform, as server actions
- [components/tree/EditorPanel.tsx](components/tree/EditorPanel.tsx) -- advanced existing-person relation and partnership tools; ordinary creation starts from a selected profile through Quick Add
- [lib/tree/neighbourhood.ts](lib/tree/neighbourhood.ts) -- who lights up on hover, traversing through union dots
- [lib/tree/relatives.ts](lib/tree/relatives.ts) -- one person's family in ROLES (parents, partners, children, siblings, relations), for the detail panel. Distinct from kinship.ts, which answers what somebody is to the VIEWER: open your grandmother's panel and it must list HER children, not yours.
- [lib/tree/collapse.ts](lib/tree/collapse.ts) -- expand/collapse as REACHABILITY, not subtraction. A family tree is a DAG, so "the subtree below X" is not a set X owns.
- [lib/tree/editable.ts](lib/tree/editable.ts) -- WHICH row an edit writes to. A fused person is several rows; the fused id is the smallest, which is frequently not yours.
- [lib/tree/person-patch.ts](lib/tree/person-patch.ts) -- which columns an edit actually writes. Absent key means leave alone; blank key means erase.
- [components/tree/PersonEdit.tsx](components/tree/PersonEdit.tsx) -- the edit form behind the panel's pencil, including your own record
- [lib/tree/kin-plan.ts](lib/tree/kin-plan.ts) -- "add a father" as a PLAN: which union to find or create, how many people, what sex the role implies. Pure, because whether a father lands in the union that already holds the mother is a claim about a family.
- [components/tree/QuickAdd.tsx](components/tree/QuickAdd.tsx) -- the per-card `+` and the role sheet. Roles are grouped by WHERE the person lands (above, beside, below); a full parent slot says both parents are recorded instead of offering a refusal; everything past the name sits behind a More-details disclosure. Adding a partner asks married / partners / not known, honoured only when the add creates the union.
- [lib/tree/paths.ts](lib/tree/paths.ts) -- connector geometry as strings: the marriage line, the rounded elbow, the sibling bracket, the vertical-tangent curve, the orbit bow. Pure, so a corner that inverts can be measured rather than squinted at.
- [lib/tree/radial.ts](lib/tree/radial.ts) -- the ORBIT arrangement: one person centred, everybody else on rings by relationship distance. Answers "who surrounds this person", which the layered pedigree cannot, since there distance encodes generation.
- [components/tree/ViewControls.tsx](components/tree/ViewControls.tsx) -- tree/orbit arrangement plus the 3D depth switch, two controls because they compose
- [lib/tree/feed.ts](lib/tree/feed.ts) -- `familyFeed()`, the family activity feed as a pure derivation over row timestamps the `TreeView` already carries (earliest source `createdAt`, latest `updatedAt`, a union's `createdAt`) -- no events table, no schema change, capped at 80
- [components/tree/FeedPanel.tsx](components/tree/FeedPanel.tsx) -- the feed rail, wearing the detail panel's own shell (bottom sheet on a phone, right rail on a desktop); a row travels to its person and opens their panel, and the feed waits underneath
- [components/ui/BrandFrame.tsx](components/ui/BrandFrame.tsx) -- the framework-neutral archival shell shared by the Next and Vite landing surfaces; auth and demo actions arrive as runtime-specific slots
- [lib/theme.ts](lib/theme.ts) -- theme + motion preference, and `THEME_SCRIPT`, the render-blocking inline script that resolves the stored theme before first paint
- [components/tree/TreeCanvas.tsx](components/tree/TreeCanvas.tsx) -- canvas geometry, focus, collapse and framing; consumes but does not own the durable viewed person
- [components/tree/TreeStage.tsx](components/tree/TreeStage.tsx) -- owns `viewedId` above the keyed React Flow provider and persists the viewed person's source ids in session storage, so search, feed, cards, LOD changes, Tree/Orbit remounts and mine/combined SPA navigation cannot disagree about the subject
- [components/tree/AccountMenu.tsx](components/tree/AccountMenu.tsx) -- authenticated identity, family access and the server-backed Sign out action; absent in demo mode because there is no session to revoke
- [components/tree/PersonDetail.tsx](components/tree/PersonDetail.tsx) -- side panel on a pointer device, bottom sheet on a phone, ONE component for both
- [components/tree/sheet-motion.ts](components/tree/sheet-motion.ts) -- one responsive motion source for right-to-left desktop rails and bottom-up mobile sheets
- [components/tree/TreeShortcuts.tsx](components/tree/TreeShortcuts.tsx) -- the keyboard bindings AND the help sheet, from one array so they cannot drift
- [components/tree/TreeStates.tsx](components/tree/TreeStates.tsx) -- loading, error, alone, search-empty, offline
- [components/tree/ThemeControls.tsx](components/tree/ThemeControls.tsx) -- light/dark/system plus the motion switch
- [app/globals.css](app/globals.css) -- design tokens; do not hardcode colours in components

## The data model, in five sentences

Read this before adding a table or a query -- most "obvious" schema changes here are wrong.

1. **A family tree is a DAG, not a tree.** Two hierarchical edge kinds (partner, parent-child), and the same human can be described by several users independently.
2. **Parentage hangs off a `unions` row, never a (father, mother) column pair.** Remarriages, half-siblings, single parents, and adoption then fall out for free instead of each needing a special case. A child can belong to two unions (birth and adoptive), which is why parentage is not a column on `people`.
3. **`people` is a claim, not a person.** "Your grandfather and my grandfather are the same man" is a consent-gated `person_links` row, never a destructive rewrite -- both owners keep their rows and unlinking restores separate views.
4. **`person_relations` holds every non-hierarchical connection** (cousin, friend, colleague, mentor, neighbour...) as one row per pair per kind. It is a separate table from `unions` because a friendship carries no generation, so it must never reach the layout engine.
5. **`contact_details` is rows, not columns.** One row per channel per person, each carrying its own visibility. A phone column could not hold two numbers and would need a migration every time a new platform appeared.

## Gotchas

- **An edit writes to a SOURCE row, never to `FusedPerson.id`.** The fused id is the smallest member id (a uuid comparison, chosen in `fuseTrees` so output never depends on input order), so on a person described by two families it is the FAR family's row about half the time -- and `treeIdForEditablePerson` would refuse the write, telling somebody they may not edit their own record. `editTarget()` in [lib/tree/editable.ts](lib/tree/editable.ts) resolves the contributing row that sits in a tree the viewer may write to. Verified against the exact trap: a merged person with fused id `aaa` (theirs) resolves to `zzz` (mine). It takes a LIST of editable trees rather than the view's single `editableTreeId`, because a viewer can hold grants on several graphs and a merged person can carry a row in more than one.
- **A form must be prefilled from the TARGET row, not from `primary`.** `primary` is whichever contributing row won the display contest (the viewer's own tree if present, else most recently updated), so on a merged person it can hold the far family's values -- and a form prefilled from their row copies their data into yours the moment you press save. A corruption with no visible tell, which is why `editInitialValues()` exists rather than reading `person.primary` at the call site.
- **`updatePerson` patched by KEY PRESENCE, and the version that did not was silently destructive.** It wrote every column unconditionally, and `orNull(form.get("x"))` is null for a field the form never rendered -- so saving a compact edit NULLED `birthFamilyName`, `deathPlace` and `sourceNote`. A birth surname is searchable ([lib/tree/search.ts](lib/tree/search.ts)), so that was data loss on a field the user could not even see. The rule now: an ABSENT key is left alone, a key present but BLANK is a deliberate erasure. That is the only rule under which a partial form is safe, and it lets one action serve a compact quick-edit and a full form. The decision lives in [lib/tree/person-patch.ts](lib/tree/person-patch.ts) so it is pure and could be asserted rather than eyeballed (12 unit tests plus live checks proved birth surname, source note and occupation survive a four-field edit, before the suite was removed).
- **`verification` is not user-editable, and its absence from the edit form is the point.** It is a provenance claim the card renders as a tick, so letting somebody mark their own row `documented` would turn the strongest evidence level into a self-assessment -- which is exactly what the separate `self_confirmed` level already means without overstating it. `treeId`, `claimedByUserId` and the `verified*` columns are omitted for the same reason: a tree id would move a person between graphs.
- **`basePath` rewrites `<Link>`, assets and `redirect()`, but NOT `NextResponse.redirect(new URL(...))`.** That builds an absolute URL from the request origin and Next leaves it verbatim, so under a `/kinfolk` mount `app/demo/route.ts` sent visitors to `/tree` -- which 404s. The shared demo link was broken on the deployed site and fine everywhere else, which is the worst way for it to break. The handler applied `NEXT_PUBLIC_BASE_PATH` by hand and was verified end to end (`/kinfolk/demo` -> 307 -> `/kinfolk/tree` 200). Historical now: the mount-path support was removed with the Vercel app on its own origin, so the handler redirects to plain `/tree` -- but the trap is Next's, and it is waiting if a mount ever returns.
- **On a shared domain a cookie must be scoped by PATH, not just named.** The SPA sits at `sagargupta.online/kinfolk` beside other projects on the same host, and cookies are scoped by host and path only -- never by port, never by project. Namespacing the session cookie's NAME stops this app reading a neighbour's; scoping its `path` to the mount stopped a neighbour reading a live session token of ours. Partly historical: with the mount-path support removed and the Vercel app on its own origin, the session cookie's path is back to `/`. The namespaced name stays, and `secure` is still derived from `NODE_ENV` rather than hardcoded `false` -- which was correct for `http://localhost` and wrong the moment it deployed.
- **Only the SPA carries a mount path now.** Pages builds the Vite SPA with `GITHUB_PAGES=true`, so its asset base is `/kinfolk/`, and copies `index.html` to `404.html` for deep links. Vercel stays at its own origin, and the Next app no longer reads a base path at all -- the `NEXT_PUBLIC_BASE_PATH` support was removed in the 0.2.0 rework. Pages calls Vercel through `VITE_API_BASE_URL`; it does not proxy the Next app and needs no apex migration.
- **The Neon HTTP driver has NO transactions, so `db.transaction()` throws at runtime while type-checking perfectly.** `lib/db/client.ts` uses `drizzle-orm/neon-http`, whose session throws "No transactions support in neon-http driver" the moment a transaction opens. The first `addRelative` wrapped its five writes in one and would have failed on every single use; a live smoke script (`pnpm db:smoke:kin`, since removed) is what caught it, because nothing offline can. Multi-write actions must therefore be SEQUENCED, with database predicates arbitrating races: quick-add claims a nullable partner slot in the `UPDATE ... WHERE slot IS NULL RETURNING` statement, tries the other slot, and deletes only its fresh unattached person if both lose. Never compensate after a successful claim or from a broad catch -- deleting a partner cascades through the union.
- **A submitted blank enum and an invalid enum are different facts.** Blank means "use the honest default" (`unknown`, or the action's documented narrow default); invalid nonblank input refuses the entire action before the first write. Falling invalid edits back to `unknown` silently erased valid stored facts while reporting success, and letting Postgres reject them turned ordinary validation into a 500. `oneOf()` returns a checked result, and every caller must unwrap it.
- **Escape dismissal is one LIFO stack, not one window listener per panel.** [components/tree/escape.ts](components/tree/escape.ts) owns the mounted surface order, so one Escape closes only the top account/search/legend/editor/share/feed/detail surface. A new dismissible overlay registers there; adding its own `keydown` listener recreates the multi-close bug and makes mount order observable.
- **CSS-only canvas state must never enter the ELK layout dependency graph.** Depth, pointer capability, focus, dim and lit are class rewrites, not geometry. [components/tree/TreeCanvas.tsx](components/tree/TreeCanvas.tsx) keeps layout on data/arrangement/detail inputs, reapplies classes after `layoutEpoch`, and updates draggable state separately. A new object/array dependency must have stable identity or a hover/toggle can re-run layout and move the viewport.
- **The current subject is `viewedId`, not React Flow selection, the open detail id, the relation pin or the signed-in self.** [components/tree/TreeStage.tsx](components/tree/TreeStage.tsx) owns it above the provider keyed by arrangement and LOD; search, feed, cards and the history docket all update that one value. The canvas derives its `Viewing` ticket, trail, collapse anchor, framing target and orbit centre from it. `selfId` remains a quieter identity marker and gets a separate `You` travel control only when it differs. A fused id is not durable across a full SPA navigation: mine-only Russell is `m1` while the combined view calls him `c1`, so the stage stores his contributing source ids under `kinfolk.viewed-sources` in `sessionStorage` and resolves the best matching fused person after each load. Persisting only `viewedId` silently resets the subject when the view boundary changes.
- **Pedigree rank is a household invariant applied after ELK.** `alignPedigreeRows()` in [lib/tree/layout.ts](lib/tree/layout.ts) union-finds partner-connected people, ranks the household DAG parent-before-child, preserves ELK's crossing-minimised x order, packs partners contiguously with a tighter internal gap and uses larger gaps between households. The internal partner gap never becomes narrower than the union mark plus clearance. A remarriage or fused household can make true endpoint adjacency impossible; `placeUnionJunctions()` then routes its junction through a clear generation-gap lane instead of drawing a marriage rail through the intervening card, and a direct childless partnership uses the same safe-lane rule. Contradictory cycles take the total-render fallback rather than dropping a person. Do not replace this with directed partner layers or hardcoded generations, and keep structured nodes non-draggable or one pointer move can invalidate every rank and rail.
- **A row packed on its OWN extent throws away ELK's parent-over-children alignment, and the row must be packed deepest-first to keep it.** `alignPedigreeRows()` originally centred each generation on its own ELK extent at minimum gaps, consulting nothing below it -- so a couple sat a mean 775px from its children's midpoint (median 600) on the sample tree, and on a real graph the viewer's parents were 807px right of him and his sisters. The parent row also packed to 3761px while the children it had to reach spanned 5541px, 1.47x wider, so a couple in the middle had 56px of slack against a 1445px journey and no post-hoc nudge could rescue it. Rows are therefore packed DEEPEST-FIRST (the row below is final, so a target cannot move underneath), each household is placed over its own children, and the gap tiers are a FLOOR rather than a target -- a generation is as wide as its descendants make it. Two traps inside this: a one-directional sweep CANNOT centre a family whose children lie to its left (one couple at x=8098 had children at x=5929, and pushing right pinned 16 of 34), so the row is re-ordered to follow its children before any gap is enforced and resolved by bidirectional relaxation; and perfect centring is sometimes arithmetically impossible, since the widest row needs 11228px while its children span 7854px. When it cannot hold, the minimum gap wins -- an off-centre parent is cosmetic, two overlapping cards is a broken canvas. Measured live: 21 of 34 couples within 10px (was 6), 26 within 200px, 0 overlaps.
- **Gap tiers are ratios of `metrics.gap`, so a level's gap has to make the TIERS legible, not merely stop labels touching.** Partner / household / family boundary run 22 / 56 / 141px at the card level, each step roughly 2.5x the last, so the eye groups on the largest gap before following a line; two tiers put a marriage and a family boundary both at 56 and fourteen people in one row read as one undifferentiated strip. The boundary is where the parent union changes, which is data rather than a heuristic on positions. The dot level's gap went 18 -> 30 for exactly this reason: at 18 the tiers computed to 20 / 35 / 79 against a 56px card, a partner gap a third of a card wide, so the level whose entire purpose is an overview showed no grouping at all.
- **Phone chrome needs disjoint pointer lanes, not only different z-indexes.** Search owns the upper-left lane, the editable dock starts below it, and the viewing docket sits below the active top chrome. Detail framing measures those real elements rather than assuming one fixed inset. While a detail/feed sheet is open, the right-side command dock becomes `visibility: hidden`, transparent and `pointer-events: none`; opacity plus pointer blocking alone would leave invisible controls in keyboard and switch-navigation order. A higher transparent or glass panel still intercepts a real click even when the control beneath is visibly unobscured; verify with real pointer and Tab input, not only an element-level `.click()`.
- **A Playwright full-page capture can duplicate this fixed-height app while stitching tiles.** Diagnose with `innerWidth`/`innerHeight`, element counts and a viewport screenshot before treating the repeated image as a duplicate React render. The live DOM has one workspace; the capture artefact does not.
- **A quick-add is relationship-first, and `planKin` in [lib/tree/kin-plan.ts](lib/tree/kin-plan.ts) is why it can be.** Recording a father through the raw primitives took four steps and a schema lesson -- create person, create union, add partner, attach child -- and doing it twice for a father and a mother produced TWO single-parent unions, which draws two junctions and no couple. That is the worst class of bug here: it looks like a rendering fault, so nobody suspects the data. The planner finds an existing parent union with a free slot BEFORE creating one, which is the whole fix, and it is pure so the rule could be asserted rather than eyeballed (23 unit tests plus 10 live checks in `scripts/smoke-kin.mts`, all removed with the suite in the 0.2.0 rework).
- **A gender field is drawn ONLY for the three roles that imply nothing, and a posted `sex` can never override one that does.** Six of the nine quick-add roles state a sex ("father" is male), so a picker there would be a field whose only use is to contradict the button just pressed. But `partner`, `child` and `sibling` genuinely imply nothing, and the first version shipped without the field at all -- so those three were permanently `unknown` with no way to record what somebody knew. `addRelative` honours a posted `sex` only when `ROLE_SEX[role] === "unknown"`: the field cannot exist in the form for a gendered role, so a value arriving alongside `role=father` is a forged post rather than a choice. Was pinned by tests asserting the override is refused for all six, until the suite was removed. Deliberately NOT adopted from the research: FamilySearch's rule that sex cannot stay unspecified once somebody is linked as a spouse or parent, and Gramps' gender-filtered parent pickers -- both contradict a schema whose `sex` defaults to `unknown`, and a gender filter here would hide most of the graph from its own picker.
- **Deriving sex from a chosen ROLE is allowed; deriving it from a NAME is not.** The standing rule forbids reading "Maria" and storing `female`, because the user never said anything. Clicking "father" IS a statement about the person being added, so `ROLE_SEX` stores what they said -- and storing `unknown` instead would contradict them. `partner`, `child` and `sibling` stay neutral because those words genuinely imply nothing.
- **A bare birth year goes in `birthDateApprox`, never `birthDate`.** `birthDate` is a real `date`, so "1952" is not a value it can hold, and coercing it to 1952-01-01 invents a birthday nobody recorded -- exactly the false precision the separate `*Approx` columns exist for. `birthYearColumns()` also bounds the year (1000-2200), because "19" and "20255" are typos that would be indistinguishable from a real value once stored.
- **A batch of children gets NUMBERED placeholders, and no shared dates.** `addPerson` rightly refuses a nameless person, and a nameless row renders as "Unknown" indistinguishable from every other, so a batch names them "Son 1", "Son 2" against an offset of what the union already holds -- otherwise a second batch produces two people called "Child 1". A birth year is never applied to a batch: stamping five children with one year asserts they were all born in it.
- **An edge that meets a union must terminate on the dot's CENTRE, which React Flow will not do for you.** It anchors edges to HANDLES and offsets handles outward from the node box, so on a 12x12 junction a partner edge stopped 9px above the dot and the child edge began 9px below: measured 173 and 191 against a centre of 182, an 18px dead zone with the dot floating in it, which is the visibly broken line. The edge cannot fix it from inside, because by then `sourceY`/`targetY` ARE the handle positions -- so `TreeCanvas` passes each junction's centre down as `sourceHub`/`targetHub` and every generator snaps to it. Verified live: 150 of 150 junction endpoints on their dot centre, 0 off.
- **Testing `data` for truthiness is not the same as testing the field a branch needs.** Once every edge carried `data` for the junction anchor, partner edges (which have no sibling bar) fell into the elbow branch and read `bar.barY` as `undefined` -- emitting a path of literal `NaN`s whose `getTotalLength()` is 0, so the couple's line vanished entirely. The guard is `typeof bar.barY === "number"`.
- **A couple is joined by a horizontal MARRIAGE LINE at mid-card height, drawn by `partnerPath()` in [lib/tree/paths.ts](lib/tree/paths.ts), and the junction sits ON it (2026-08-07).** ELK places a union junction as an ordinary node in the generation gap to minimise crossings -- the right objective for a layered graph and the wrong position for a marriage, since the convention every hand-drawn pedigree uses is a line joining the couple with the children descending from its midpoint. `placeUnionJunctions()` in [lib/tree/layout.ts](lib/tree/layout.ts) (replacing `centreUnionDots()`) moves a couple's junction onto the line -- y at the partners' mid-card height, x from the couple's midpoint as the STARTING guess only. Since PR #32 the bead's x is clamped by `nearestClearPoint()` into the nearest clear gutter between the partners' facing edges, because a midpoint is only clear space when the couple is adjacent: with somebody laid out between them (a remarriage chain, a fused graph) it is the middle of that person's card, and 5 of 34 beads sat on people's faces. The partners' own cards count as blockers, so a bead never paints on a person card; a couple with NO clear gutter keeps ELK's y in the generation gap instead, the pre-marriage-line rendering. A single-parent union is centred under its one parent, where the honest picture is the classic straight drop. The junction bead wears the line's own colour and draws status in the genogram's conventions: a solid point for an intact partnership, a hollow bead for widowed, a double slash through the line for divorced or separated. Measured live: 68/68 partner edges flat, 34/34 beads on the line, 82/82 child drops leaving from the bead's centre, 0 NaN. A post-layout nudge for the same reason `siblingBars` is: the skeleton must not shift for cosmetics.
- **The dot level carries a FIRST name, and the width is measured rather than chosen.** A bare mark showed the shape of the family and nothing else, so finding anybody meant going back to cards and losing the overview. Across all 117 people at 9px mono a first name needs 45px at p90 and 51px at the widest, where a full name needs 84px -- which would make this level nearly as wide as the compact row it exists to be smaller than. Hence a 56x34 slot, and `LEGIBLE_ZOOM.dot` raised 0.18 -> 0.42: at 0.18 a 9px label renders 1.6px tall, which is present in the DOM and unreadable, and that reads as a rendering fault rather than as an overview.
- **A connector's corners live in [lib/tree/paths.ts](lib/tree/paths.ts), because a hand-built path forgets to round them.** The sibling-bar route was assembled from `L` commands only, so 82 of 150 edges turned through hard 90 degree corners while `CORNER_RADIUS` sat unused in the fallback branch -- and one shared bar ran 6013px against a 200px median, a line crossing the whole canvas. Now every turn is a clamped quadratic (clamped to half of each leg, or a child almost directly below its parent bulges sideways past where it started), a family wider than `MAX_BAR_SPAN` is drawn as per-child curves instead of a bracket nobody can take in at once, and the bracket's ends turn DOWN so it closes as one mark rather than reading as a rule. Verified live: hard-corner paths 82 -> 3, and the three remaining are straight vertical drops with no corner to round. `MAX_BAR_SPAN` is 1600, raised from 900 once parent-over-children alignment landed: while a bar's width was partly layout DRIFT, a narrow limit rejected genuinely wide families and genuinely misaligned ones alike -- including a set of FOUR siblings at 1052px, which is precisely what a bracket is for. With each couple centred on its own children a wide bar means a large family and nothing else, and 1600 is a little over a 1440px viewport, so a surviving bracket is one the eye can hold at fit zoom while a seven-child span (4847px measured) still takes the curves.
- **The orbit's angles were the defect, not its curves, and three tuning passes on the bow proved it.** Placing each ring by index alone gave a median edge span of 122 degrees with 26 of 34 edges crossing more than 90 -- a parent on ring 1 and its child on ring 2 got independent positions and routinely landed on opposite sides of the circle, so every edge had to cross the middle. Bowing them harder just turned chords into loops. Wedge allocation (each node placed inside its predecessor's angular sector) took the median to 1 degree and 0 over 90. **When a graph looks tangled, measure the angular separation before touching the path generator.**
- **In the orbit, wedges decide ORDER and even spacing decides GAPS, and conflating them makes the radius unbounded.** Sizing a ring on its tightest allocated slot solves `slot * r >= width + gap`, which grows without limit as a slot narrows: one crowded family took the outermost ring to 3641px, then to 9291px with a 17024px edge once unions were collapsed away. Capping the radius instead broke the no-overlap guarantee, because the two cannot both hold. So `ringRadius(count)` sets the radius and nodes are placed at even intervals in wedge order -- families stay contiguous (short edges) while every card gets an equal slot (no overlap). Final: max radius 1680px, longest edge 2817px, and 19/16/21/19 nodes across the four quadrants.
- **The orbit places PEOPLE only; union dots are traversed through, never ringed.** CLAUDE.md already says a union dot is "the MARRIAGE, not a route to the children", and this view takes that literally: counting a junction as a hop put a spouse alone on ring 1 while parents and children shared ring 2, and it spent a full 168px ring step on a 12x12 mark every other generation. `collapseUnionEdges()` rewrites partner and child edges person-to-person (2 + N becomes 1 + 2N, affordable because the orbit shows at most four rings), and `hopsFrom` takes an `isUnion` predicate so a ring means "people I am N relationships from".
- **3D is applied to the CARD, never to the pane, and per-card is also the version that works.** A canvas-wide `perspective` + `rotateX` was the obvious implementation and is wrong: React Flow owns each node wrapper's 2D `translate`, so tilting an ancestor leaves the layout and the picture disagreeing about where a card is, and the SVG brackets (drawn in flat coordinates) no longer meet the cards they join. Per-card was verified empirically rather than reasoned about -- a card under `rotateX(45deg)` measured 132px -> 94px, three hit-probes down its face all landed inside it, and its click still fired, because the browser's own hit-testing follows a CSS transform. Confirmed on the real canvas at 151 tilted nodes: 13 of 14 visible tilted cards clickable, the exception sitting under a toolbar panel.
- **A touch target inside a scaled canvas must also be CAPPED at the size of what it belongs to.** `calc(44px / var(--kf-zoom))` is correct for the floor and unbounded upward: at 0.4 zoom that is 110 layout pixels, which measured wider than the whole 32x17 card, so `elementFromPoint` at a card's centre returned the fold BUTTON and clicking a person opened a fold instead of their panel. A control may overhang its card's edge; it may never eclipse it.
- **The MAJORITY state gets no colour, on every surface that draws it.** 94 of 117 people in the sample tree are living, so a green living-rail put a saturated 7.36:1 stripe on 80% of the cards -- brighter than the NAME above it (rail luminance 0.366 against the card's 0.0065) -- and at fit zoom the tree read as rows of green dashes rather than as a family. The same fact wearing the same colour appeared three times: the card rail, 93 of 152 minimap rects, and 93 of 117 dots in dot mode, where one amber pixel for the viewer had to compete with a green mass. Living is now unpainted (card), `--color-edge` (minimap and dots); deceased keeps the quiet grey because it is the minority (23), and `unknown` keeps its hairline because an unrecorded status is a real stored value. Every individual green cleared its contrast floor -- only the screenshot showed that 94 of them shared a canvas, which is the same lesson as the presence-ring fog reached from the other side.
- **A touch target inside the canvas must be sized in `1 / zoom`, because everything in `.react-flow__viewport` is in a scaled layer.** A `size-11` (44px) fold button measured 35px on screen at 0.785 zoom and would be 7px at the 0.15 floor. The fix is `calc(44px / var(--kf-zoom, 1))` with `--kf-zoom` published onto the viewport element by TreeCanvas, so the hit area is 44 SCREEN px at every zoom (verified live at 0.785, 0.654 and 0.545) while the visible pill stays 20x14. Two traps: a Tailwind arbitrary-value utility (`size-(--kf-hit)`) resolves the variable where it is DECLARED, so it read the `:root` fallback of 1 and still measured 35px -- the value has to be referenced inline on an element inside the viewport; and the offset must scale with it (`calc(-22px / var(--kf-zoom, 1))`) or the pill drifts off the card edge as you zoom.
- **A bottom sheet on a phone gets 55dvh, not 70.** At 375x812, 70dvh is 568px of sheet against 244px of canvas -- and the header, demo banner and search box already own most of that, so the card you just tapped was itself covered. A detail panel that hides its own subject has lost the thing that makes it a panel rather than a page, since this one is deliberately non-modal so a person can be read against the family around them. 55dvh keeps 365px of canvas (the subject's row plus the one above it) and the sheet still scrolls to every section.
- **Collapse blocks the UNION, never the person, and that asymmetry is the data model showing through.** Parentage hangs off `unions`, so a mother's own outgoing edges are the PARTNER edges joining her to her partnerships -- block those and the couple comes apart, leaving a marriage rendered with one participant. Folding descendants therefore blocks the unions she partners in, which also means folding a mother's descendants folds the father's: they are the same children, and the union is the thing that says so. Upward is the opposite (a person's parent unions ARE their up-edge targets), so the block sits on the person. Found by a test that asserted both halves of the contradiction, before the suite was removed.
- **A collapse walk seeded from the ROOTS cannot fold ancestors, because the roots are what an upward fold hides.** The first implementation let every folded grandparent back in through the front door and an upward collapse silently did nothing. `entryPoints()` disqualifies anything behind a blockade and seeds from everybody else, so a fold stays LOCAL -- collapse one person in a tree of 117 and the other 116 are still entry points, where seeding from the collapsed node alone would leave a canvas holding one branch. The viewer and every collapsed node are anchors regardless, or folding your own ancestors empties the graph.
- **A union dot is the MARRIAGE, not a route to the children, so it survives a fold that hides them.** It is dropped only once fewer than two of the nodes it joins remain -- which is the single-parent case, where the dot would otherwise hang below the parent pointing at nobody.
- **Tailwind 4 scans markdown and CSS comments for class names, so documenting a broken utility COMPILES it.** The square-bracket gotcha note in this very file emitted a live `--tw-duration: --duration-base` rule into the production stylesheet (verified 2026-07-31 by grepping `.next/static/**/*.css`: 1 dead rule, traced to CLAUDE.md line 127, 0 after rewording). Describe a broken form in prose; never spell it.
- **`@theme` freezes a token's value; `@theme inline` emits the reference.** With two schemes on `[data-theme]`, a plain `@theme` copies whatever the attribute said at parse time into `:root`, so every utility is stuck on one scheme and switching does nothing. `@theme inline` compiles `bg-canvas` to `background-color: var(--kf-canvas)`, which resolves live. Verified in the built CSS rather than by looking: the utility must read `var(--kf-*)`, never a hex.
- **A light scheme is not an inversion.** The 0.3.0 palette treats warm paper and deep evergreen as separate surfaces, with scheme-specific ink, edge, accent and alpha values. The archive amber keeps a separate `--color-accent-ink` because a hue usable as a line is not automatically usable as small text. Re-measure text against 4.5:1 and meaningful graphics against 3:1 whenever a canvas, surface, token or alpha changes; numeric claims from the pre-0.3.0 palette are not reusable.
- **The motion switch is in the app, not `prefers-reduced-motion`, and that is deliberate.** These animations carry information -- the stagger shows generations assembling oldest first, the draw-on shows the skeleton growing parent to child, the pulse shows which way parentage runs -- so an OS flag set years ago to stop advertising banners jumping would silently delete a data channel. `data-motion="off"` on `<html>` is written by a discoverable, reversible control. The gate uses `0.01ms`, never `0`: a zero-duration animation never fires `animationend`, so anything awaiting it hangs.
- **The theme script must be a raw inline `<script>` in `<head>`, not `next/script`.** `beforeInteractive` still defers past first paint, which is exactly the white flash the script exists to prevent. It also needs `suppressHydrationWarning` on `<html>`, since it mutates two attributes before React hydrates -- a difference we cause on purpose.
- **Auth.js Drizzle adapter matches account columns by TS property name in snake_case.** `refresh_token`, `access_token`, `expires_at`, `token_type`, `id_token`, `session_state` stay snake_case in [lib/db/schema.ts](lib/db/schema.ts) even though the rest of the schema is camelCase. `expires_at` must be `integer` (OAuth epoch seconds), not a timestamp. Renaming them to camelCase produces a `DefaultPostgresAccountsTable` type error.
- **`experimental.useTypeScriptCli: true` in [next.config.mjs](next.config.mjs) is load-bearing.** TypeScript 7 removed the compiler API Next reaches for directly; without the flag dev boot throws "TypeScript 7.0.2 does not provide the compiler API required". Remove it once Next supports TS 7 natively.
- **pnpm 11 reads build-script approval from [pnpm-workspace.yaml](pnpm-workspace.yaml) (`allowBuilds:`), not `package.json`.** A `pnpm.onlyBuiltDependencies` field in package.json is silently ignored and installs fail with `ERR_PNPM_IGNORED_BUILDS`.
- **Dates are `date`, never `timestamp`.** A birthday has no timezone; storing it as an instant shifts people across midnight depending on who reads it. Fuzzy dates go in the separate `*Approx` text columns ("about 1890").
- **`unknown` is a real stored value**, not a missing one. Genealogy is mostly incomplete data and forcing a guess corrupts the record.
- **Only accepted links reach `fuseTrees()`.** A pending proposal must never change what anyone sees; callers filter on status.
- **A relation edge must never influence node placement.** `FlowEdge.layout` is the discriminator and [lib/tree/layout.ts](lib/tree/layout.ts) filters on it before handing edges to ELK. Feed a "friend" edge to the layered algorithm and that friend gets shoved into a lower generation, which silently misrepresents the family. A layout test asserted the family skeleton's positions are identical with and without relation edges present; the suite is gone, but the invariant stands -- if positions shift when the overlay is fed in, it leaked into the skeleton.
- **Withholding social edges from ELK creates a second problem, which `anchorFamilylessNodes()` in layout.ts solves.** A friend with no parents and no partner is an isolated node to ELK, so it lands in the FIRST layer and renders as somebody older than the grandparents. The post-layout pass moves those people onto the row of a contact they know. It deliberately runs after layout instead of as an ELK constraint, so the skeleton cannot shift to accommodate a friend.
- **Relation edges are `pointer-events: none` on themselves AND every descendant.** They render above the cards (`zIndex: 1001`) so their labels are not painted over, which means an edge label's `<rect>` sits on top of a card. React Flow sets `pointer-events: all` on labels, and that beats a `none` on the ancestor group, so the `*` selector in globals.css is load-bearing: without it a label swallows the drag that should move the card.
- **Hover BRIGHTENS the presence ring; it must not grow it.** The `.kf-lit` rule once widened the ring by 7px on top of its resting spread, turning a highlight into a second border and pushing the card's apparent edge into layout space. It now adds only 1px; the theme's lit colour carries the signal. Keep that colour visibly stronger than the resting ring in both schemes and re-measure after palette changes.
- **A lit family edge carries a travelling pulse, and it is a SECOND path rather than a dash on the first.** The base path is already animating its draw-on and taking colour and weight from the cascade, so layering avoids an override fight and keeps the line solid underneath instead of becoming a dashed line while a dash moves through it. `0.12 0.88` against a normalised `pathLength` is ONE bright segment chasing a long gap, not a train of dots -- a repeating pattern in motion reads as data flowing through a pipe, where one segment reads as the eye being led parent -> child. The offset animates 1 -> 0 so it runs downwards; reversed, it would assert that children produce their parents.
- **The pulse path is unconditional, and `stroke: none` is why that is free.** A `lit` prop on `FamilyEdge` would put focus state into the memo the layout effect depends on, re-running ELK on every hover -- the same trap the split edge arrays exist to avoid. So all 150 pulses are always mounted and CSS switches them on from the ancestor's `.is-active`. A path with no stroke is skipped at paint; 150 transparent-but-painted strokes would cost a compositor pass each. Verified live: idle pulses report `stroke: none` and `animation-name: none`, and a lit one steps `0.766 -> 0.549 -> 0.332` over two 300ms samples.
- **`aria-hidden` on an SVG `<path>` or `<g>` will not pass Biome, and the rule is right.** `noAriaHiddenOnFocusable` fires on both, and `focusable="false"` does not clear it. Do not suppress it: a bare `<path>` has no role and no accessible name, so there is nothing in the accessibility tree to hide, and the edge's name already comes from the `ariaLabel` React Flow puts on the group while `edgesFocusable={false}` keeps the subtree out of the tab order. The attribute was redundant, so it is absent rather than ignored.
- **A relation label is revealed by hovering the PERSON, not the edge.** Since the overlay takes no pointer events, focus comes from `onNodeMouseEnter`/`onNodeClick` in [components/tree/TreeCanvas.tsx](components/tree/TreeCanvas.tsx). Node focus is applied in its own effect that only rewrites `className` -- folding it into the layout's edge memo would re-run ELK on every hover, because the layout effect depends on that memo. Edge focus is no longer written to state at all: the rendered edge array is derived, so applying focus there too would store the same fact twice.
- **A revealed relation shows its label immediately; it does NOT wait for a second interaction.** The old rule started labels at `opacity: 0` until `is-active`, which was right when 52 relations were permanently drawn (52 labels would have been text over every card). But the reveal already consumed the hover and the tap, so there is no second gesture left to ask with -- and a line appearing with no word on it says a connection exists while withholding the one thing that makes it meaningful. The label fades in on a delay matching the draw-on, so the word lands as the line reaches the far card.
- **Directed relations are stored one way only.** "mentee" is not a stored value; it is the `mentor` row read from the other end via `relationLabel()`. Adding an inverse enum member would let the same fact exist as two rows with nothing to reconcile them.
- **Symmetric relations get id-sorted endpoints via `canonicalPair()`.** That is what makes the unique index able to reject a duplicate friendship recorded from either side. Directed kinds keep caller order, because A holds the role. Never insert a relation without going through `canonicalPair()`.
- **Self-relations are blocked in application code, not by a constraint.** Drizzle cannot express a CHECK inside an index, so `personAId <> personBId` is the caller's job. Fusion can also *create* a self-loop after a merge; `fuseTrees()` drops those.
- **Contact values never reach the canvas.** A card shows only which channels exist; a canvas gets screenshotted. Values default to `visibility: "tree"` (the narrowest) and require a deliberate click.
- **Phone numbers are stored as entered, never normalised.** Relatives abroad have country codes, older records have landlines, and rewriting the string loses information the owner deliberately typed. Dedupe compares trimmed + lowercased values only.
- **`next-env.d.ts` is excluded from Biome.** Next regenerates it with CRLF on every dev boot, so formatting it just loses the race. [.gitattributes](.gitattributes) pins LF for everything else.
- **React Flow error#004 ("parent container needs a width and a height") is not always noise.** While the container measures 0x0 React Flow never measures nodes, so edges render as an empty container. Framing keys off `useNodesInitialized`, not `requestAnimationFrame`, for exactly this reason.
- **`lib/db/client.ts` must stay importable with no `DATABASE_URL`.** Next collects page data by importing every route's module graph, so throwing at module scope fails `pnpm build` on a fresh clone. The client is always constructed for real and pointed at a placeholder URL whose queries throw a message saying what to do. A lazy Proxy is not an option: the Auth.js Drizzle adapter type-checks the instance it is handed at import time.
- **The session cookie is `kinfolk.session-token`, and the rename was a bug fix.** Auth.js defaults to `authjs.session-token`, and cookies are scoped by HOST, never by port -- so every Auth.js app on `localhost` reads and writes the same name. With `strategy: "database"` the stored token is a `crypto.randomUUID()` (see `@auth/core` `lib/init.js:77`), so a neighbour app running the JWT strategy leaves a 627-character JWE where a 36-character uuid belongs, and `/tree` throws `AdapterError` for a visitor who never signed in. The tell is a `session_token` param starting `eyJ` whose decoded header is `{"alg":"dir","enc":"A256CBC-HS512"}` -- that is a JWE, which the database strategy never mints, so the cookie cannot be ours. Only the session cookie is namespaced; @auth/core merges `cookies` over its defaults, so callback/csrf/pkce keep theirs.
- **Provisioning is retried on every successful sign-in, never derived from a page render.** [lib/tree/provision.ts](lib/tree/provision.ts) gives a user one starter graph and one claimed self (`verification: "self_confirmed"`, `sex` left `unknown`), then repairs tree-without-self and self-without-root partial states after transient failures. `people_claimed_user_idx` is the concurrency invariant: the insert uses `ON CONFLICT DO NOTHING`, selects the winner, and sets a root only while it is null. Auth.js calls it from `createUser` for the first attempt and `signIn` for repair; the SPA calls it on every OAuth completion. It swallows genuine failures so a convenience never costs a valid session, and the next sign-in retries. Never derive `sex` from the display name: it is stored, so a guess outlives the render that made it.
- **Pages call `sessionOrNull()`, never `auth()` directly.** Sessions live in the database, so with no `DATABASE_URL` there is nothing to look one up in and Auth.js logs `MissingSecret` on every render. Reading the demo tree needs no auth, so the guard belongs in one place.
- **`DATABASE_URL` in `.env.local` must be the DIRECT host, not the `-pooler` one.** The app talks to Neon over their serverless HTTP driver, which is happy either way, but `drizzle-kit push` opens a plain TCP connection that the pooler endpoint will not serve. One variable feeds both, so the direct host is the only value that satisfies both readers.
- **`drizzle-kit push` on Windows prints `Changes applied` and THEN crashes** with `Assertion failed: !(handle->flags & UV_HANDLE_CLOSING)` and exit code 3221226505. That is libuv tearing down the websocket after the work has committed, not a failed migration. Confirm against `information_schema` rather than trusting the exit code -- the live smoke script that used to do this was removed in the 0.2.0 rework.
- **The JSON boundary owns Date revival (0.2.3).** 0.2.2 broke the prod SPA: [lib/tree/feed.ts](lib/tree/feed.ts) called `.getTime()` on timestamps that arrive from the JSON `/api/tree` response as ISO STRINGS, so `familyFeed()` threw inside a render-path `useMemo` and the canvas never mounted -- while the server-rendered app, which never crosses JSON, worked perfectly. The fix (0.2.3): `parseTreeView()` in [lib/tree/serialise.ts](lib/tree/serialise.ts) revives every flattened timestamp -- person `createdAt`/`updatedAt`/`verifiedAt` on `primary` AND every source row, contact stamps, union `createdAt` -- so a parsed view is indistinguishable from a served one. The rule: any new `Date` field added to `TreeView` MUST be revived in `parseTreeView`, and downstream code may then assume real `Date` objects. Do NOT add per-consumer defensive parsing: a consumer that copes with strings beside one that does not is the same invisible drift as a serialiser disagreeing with its parser.
- **`view.kinship` is a `Map`, not a record.** `kinship[id]` type-checks (index signatures on a Map object) and returns `undefined` for every id, so an assertion written that way passes vacuously against an empty expectation. Always `kinship.get(id)`.
- **An accepted link is symmetric, so the FAR tree's owner loads your tree too.** Their `loadTreeView()` reports `trees: 2`, not 1 -- that is the product working, and the invariant to check is that the far tree arrives at `linked` access with its tree-private contacts stripped, not that it is absent.
- **`tsx` compiles a bare `.ts` script to CJS here**, because package.json has no `"type": "module"`, so top-level `await` fails with "not supported with the cjs output format". Name a script `.mts` to get ESM.
- **Only the INNER card animates on entrance, never the node wrapper.** React Flow owns the wrapper's `transform` for positioning; animating it fights the layout and the node lands in the wrong place. Hence `.kf-enter > *` in globals.css rather than `.kf-enter`.
- **Import `getNodesBounds` from `useReactFlow()`, not the package root.** The standalone export has no node lookup, cannot handle sub-flows, and warns on every single call.
- **The fusion reveal reads `contributingTreeIds.length`, so it is a property of the data, not a flag.** Mine-only mode produces `merged 0` and therefore no stacked sheets, automatically. The sheets carry no `z-index`: they and the card are all `z-index: auto`, so paint order alone puts the card on top, and that survives an ancestor gaining a stacking context in a way a negative index would not.
- **Generation bands come from person nodes only.** A union dot sits between two rows, so including one invents a half-generation. The entrance stagger therefore snaps each node to its NEAREST band rather than an exact y match, or every union dot gets delay 0 and pops in ahead of the couple it joins.
- **A Tailwind 4 CSS variable in an arbitrary value needs PARENTHESES, not brackets.** `duration-(--duration-base)` compiles to `var(--duration-base)`; the square-bracket form emits the literal property name instead, which is not a valid duration, so the property silently falls back to its initial value and the transition simply does not happen. Nothing warns -- not the build, not Biome, not the browser console. 34 utilities across this repo were dead this way; when adding one, confirm the computed style on a real element rather than trusting the class list. **Do not write the broken form out in prose anywhere in this repo, including this file and CSS comments**: Tailwind's scanner reads markdown and comments as class-name sources, so spelling it literally compiles a dead rule into the production stylesheet -- verified 2026-07-31, this very line was doing it.
- **`color-mix()` cannot be contrast-scored from its source token or a naive sRGB parser.** Browsers may expose the computed value as `oklab()`, and translucent glass, lattice, wash and ring marks depend on the surface behind them. Resolve the actual pair through a 2D canvas and read `getImageData`; re-measure after either scheme's token or alpha changes.
- **Feeding React Flow's own CSS variable beats overriding its selector.** Their `.react-flow__background-pattern.dots { fill: ... }` ties a `.react-flow .react-flow__background-pattern` override on specificity and then wins on source order, because their stylesheet is imported after globals.css. Setting `--xy-background-pattern-color` on `.react-flow` lands, since their rule reads it. This also means the `color` prop on `<Background>` must be REMOVED, not just left unset: it writes `--xy-background-pattern-color-props`, which they check first.
- **`.react-flow__background` renders OUTSIDE `.react-flow__viewport`.** The pane carries the pan transform and the background does not (confirmed on a live element), which is exactly what lets the edge-fade `mask-image` stay screen-fixed while the tree pans under it. Anything else placed in that SVG inherits the same non-panning behaviour.
- **The sibling bar is computed from POSITIONS, after ELK, never as an edge.** ELK routes parent-child edges individually, so twelve children produce twelve separate diagonals; the bar is the thing that makes them read as one family. Lanes are assigned widest-span-first and shared between families that cannot overlap in x, which is what keeps a generation to a single clean line -- a test asserted `barCollisions()` is 0 while the suite existed, because two families sharing a y AND an x range is the one way the bar can lie about whose child is whose.
- **A bar can never sit above its own union dot.** With a tight generation gap the lane offset exceeds the room available, and the drop line then points UPWARDS, reading as the children being the ancestors. `siblingBars()` clamps to just below the dot; a test pinned the clamped value until the suite was removed.
- **Kinship is walked from the graph, never guessed from `sex`.** `sexEnum` includes `"other"` and defaults to `"unknown"`, so `kinship.ts` returns neutral terms ("parent", "sibling") when the stored value cannot name a gendered one. Guessing to get a nicer word would assert something nobody recorded, and it is also why the gender-required layout libraries were rejected.
- **An inferred kinship is drawn a step fainter than a proven one.** `via: "in_law"` and `via: "relation"` are read off a partner's line or a stored relation rather than proven by the ancestor walk, so they get `text-ink-faint` where a walked term gets `text-ink-muted` -- the same how-firmly-drawn-tracks-how-well-known hierarchy as the provenance tick.

## Repo-specific rules

- Design is an archival field desk: warm paper in light mode, charcoal-evergreen in dark mode, editorial folio framing, ruled index records, 1px keylines, serif display type, mono + `tabular-nums` metadata, and archive amber reserved for selection and relationships. Keep opaque reading sheets over the graph, glass only for floating tool chrome, 44px minimum hit targets, responsive mobile hierarchy, and short ease-out motion.
- **Glass (`.kf-glass`) is only for chrome that FLOATS over the canvas, and the boundary is performance, not taste.** `backdrop-filter` forces a compositor layer and re-filters what is behind it every frame; on 117 cards that is 117 layers re-rasterising as the tree pans, which is the "1000+ members without lag" target broken by decoration. At most five are on screen. Cards get a hairline plus one soft shadow on hover -- hairlines remain the primary boundary everywhere.
- **Colour is declared per scheme and consumed by NAME. No component carries a `dark:` variant.** Add a colour by declaring it in both `[data-theme]` blocks and exposing it through `@theme inline`. A one-scheme token is a bug that only shows up for half the users.
- Keep graph maths in `lib/tree/` with no React or DB imports so it stays pure and assertable. The unit suite that pinned this layer was removed in the 0.2.0 rework, but the boundary stays: while it existed, the collapse and relatives tests both caught defects invisible on screen, because a person who vanishes from a canvas does not announce that they were dropped for the wrong reason.
- **A keyboard shortcut and its help row come from ONE array** (`BINDINGS` in [components/tree/TreeShortcuts.tsx](components/tree/TreeShortcuts.tsx)). A handler in one file and a documented list in another drift on the first change, and a help sheet that lies is worse than none. Single letters need the typing guard: without it, typing a name into search zooms, fits and cycles the detail level on the way through.
- Fused ids must be deterministic (smallest member id) so output never depends on input order. A test pinned this while the suite existed; do not "optimise" it away.
- Relation categories are distinguished by **dash rhythm, not colour** (kin `8 3`, care `9 3 1 3`, social `5 4`, professional `2 4`, other `1 4` -- see [app/globals.css](app/globals.css)). Colour-coding them would break the one-accent rule. All five have an explicit rule, ordered tightest rhythm to sparsest so the dash reads as distance from kinship; each rule sets ONLY `stroke-dasharray`, since weight belongs to closeness and colour to `--color-edge-soft`.
- **Every rhythm lives in a `--kf-dash-*` token, because two readers depend on it**: the edge rules and the legend's sample lines. Hardcode it in both and the next change lands in one of them, leaving the key describing a line the canvas no longer draws -- the one failure a legend cannot survive, since its whole value is being believed without checking. The active-relation rule is the deliberate exception and says so inline: its literal `5 4` is coupled to the `-18` dashoffset so the flow animation loops seamlessly.
- **The legend is census-driven, so a row never explains a mark that is not on this canvas.** [lib/tree/census.ts](lib/tree/census.ts) counts the ENABLED nodes and edges (the caller passes `visibleEdges()`, so switching the overlay off empties the relation rows) and [components/tree/TreeLegend.tsx](components/tree/TreeLegend.tsx) drops any row whose count is zero. Deliberately the enabled set rather than the momentarily-rendered one: relations are revealed one person at a time, so a census over what is literally on screen would empty every relation row the instant the pointer left a card -- a key that blanks itself while you read it. The counts answer "what is in this graph"; the Connections section's `note` answers "when will I see it", which is the row a reveal-based canvas cannot do without. Kinfolk knows 17 relation kinds and 5 verification levels and no real tree uses all of them; one row pointing at a line nobody can find costs the whole key its credibility. Rows are derived from `RELATION_KINDS`, the dash tokens, and `PROVENANCE` rather than restated.
- **A card encoding listed in the legend must be reproduced with the card's own structure, not approximated.** `.kf-presence` is `position: absolute; inset: 0` with its own `border-radius`, so a bare span wearing that class escapes its row and pins to the panel corner, and its radius beats any Tailwind `rounded-*` (plain CSS outranks a utility layer). Wrap it in a `relative` parent and pass the radius through `style`, the way [components/tree/PersonNode.tsx](components/tree/PersonNode.tsx) does. Verify a sample by reading its rect off a real element -- both bugs looked correct in the file.
- A new relation kind means three edits, in this order: the `relationKindEnum` member, its `RELATION_KINDS` spec (label, inverse, symmetric, category), and its `.is-<kind>` selector added to that CATEGORY's rule in globals.css. Never a rhythm of its own -- the rhythm names the category, so a per-kind dash would claim a distinction the legend does not make. Skip the spec and `relationLabel()` throws on read.
- **Write permission is [lib/tree/authz.ts](lib/tree/authz.ts), NEVER `accessibleTrees()` from load.ts.** The read helper grants `member` to anyone with a row, which is correct for reading and catastrophic for writing: it would let a `viewer` grant edit, and it would let a tree reached through an accepted person link be rewritten by the other family. An accepted link means "we agree this is the same human", not "you may edit my records". `editableTreeIds()` requires ownership or an `owner`/`editor` grant. Verified live: a user cannot edit another user's graph, and `assertSameTree()` refuses a cross-graph pair with a message pointing at person links.
- **A relation or union may never straddle two trees.** `assertSameTree()` enforces it, because `person_relations` and `unions` have nowhere to record the other family's consent -- that is what `person_links` is for. Without the check, one user could unilaterally write an edge into somebody else's graph.
- **`editableTreeId` on the view is a RENDERING hint, not a permission.** It decides whether the Add panel is drawn (null in demo mode and for a read-only viewer, which hides the editor rather than disabling it -- a disabled control advertises an action that can never work). Every action re-checks server-side, because a value that reached the client is a value the client can change.
- **The editor pre-fills its "from" field from the canvas selection, and SAYS so in the header.** Clicking a card already sets hover focus, so `onNodeClick` carries both. Without the header naming the picked person the select appears to change on its own, which reads as a bug rather than a shortcut. Union dots are skipped: a junction is not somebody you can relate to, and reporting one puts "Unknown" in a picker.
- **Public entry screens use [components/ui/BrandFrame.tsx](components/ui/BrandFrame.tsx); they are not independent page designs.** Next and Vite pass runtime-specific auth/demo actions into the same folio masthead, editorial hero, cross-reference vignette and archive ledger. Sign-in uses the compact form of that shell. `.kf-lattice` and `.kf-chip` remain valid for canvas loading, callback and empty states, but are no longer the landing-page visual system.
- **The landing headline puts the accent on the SECOND line only.** The product is the join, so the archive amber lands once on “Map the whole living record.” A gradient across both lines would spend the accent on decoration, while the canvas reserves that colour for “you,” selection and a revealed relationship.
- **Card width AND height both come from `NODE_METRICS`, because ELK reserves both.** `PersonNode` set only the width, so the card sized itself to content -- raising the reserved height to fit a two-line kinship term then bought nothing on screen and left the layout and the card disagreeing by 16px (reserved 92, rendered 78, measured live). The inner card needs `size-full` for the same reason.
- **The kinship line clamps to TWO lines; the name never wraps.** Measured on the sample tree: 31 of 117 kinship terms (26%) were truncated at one line, because that term is the widest thing on the card -- "great-great-uncle by marriage" needs 164px in a 142px column, where the p90 NAME needs 109px. Truncating kinship is worse than truncating anything else, since the terms share long prefixes and "great-great-gran..." could be a grandmother or a grandfather. `kinship.ts` is already maximally compressed (greats collapse to `4x great-` past three), so the fix belongs in the card.
- **Do NOT put a glyph on the name line.** Tried, measured, reverted: the sex glyph plus its gap takes the name column from 142px to 105px and pushes 9 of 117 names from fitting into truncating. The name is the one thing on a card that must never be clipped. Glyphs that are on EVERY card go on the metadata row; the name line carries only conditional marks (provenance tick, conflict shield).
- **A screenshot catches what per-element measurement cannot.** The presence ring once read acceptably in isolation but appeared on most people, turning the full canvas into fog. It now uses a quiet scheme-specific alpha (`--kf-ring-alpha`) and a capped 6px spread rather than 10. The converging edges already express connection density more precisely; the ring must remain texture, not a second card border.
- **A relation is REVEALED for one person at a time, never drawn at rest, and this replaced the whole `is-far` mechanism.** Measured on the sample graph: the 52 relation edges were 73% of all edge ink at a median span of 2214px, against the family skeleton's median of 69px (32x) -- and the data is sparse, since the median person has ONE relation and 56 of 120 have none. Fifty-two permanent canvas-crossing diagonals were the haze the cards sat in, and distance-fading them (`--color-edge-far`, `FAR_SPAN`, `#kf-taper-far`) was treating a symptom: the defect was drawing a per-person fact globally. `revealedRelations` in [components/tree/TreeCanvas.tsx](components/tree/TreeCanvas.tsx) filters to the hovered-or-pinned person, so the resting canvas is the family skeleton at ONE stroke treatment (verified live: 150 edges, 1 treatment, down from 202 edges and 15). All three far-edge machineries are deleted rather than left unreferenced -- a revealed edge is drawn at full strength however far it reaches, because the viewer asked for precisely that line.
- **The reveal is split across TWO edge arrays, and the split is what keeps ELK off the hover path.** `familyEdges` is state written once per layout; `relationFlowEdges` is a memo over the revealed person; the rendered array is a memo merging both. The family memo must read `enabledEdges`, NOT the revealed set -- the layout effect depends on it, so a memo that narrowed with the pointer would re-run ELK for the whole graph on every mouse move. Same trap as the focus effect, reached from a different direction.
- **Hover reveals, click PINS, and the pin is not a nicety.** A hover-only reveal vanishes the moment the pointer moves towards the line it just drew, which on a canvas this wide makes a long relation impossible to follow -- and on a phone there is no hover at all, so the tap is the only way in. `pinnedId ?? focusedId` is the revealed id, clicking the same card again unpins, and `onPaneClick` clears both. Search pins too: there is no pointer on a card you travelled to, and "who is this person connected to" is usually why you searched them. `lit` keys off the REVEALED id, not the hovered one, or a pinned person's lines would sit on screen with nothing lit and every card dimmed around them.
- **The card's pointer-tracked wash writes CSS custom properties, never React state.** Ported from `brand/portfolio-react`'s `GlassCard`, with three deliberate departures: its `useState` + rAF pair re-renders on every pointermove, which React Flow answers by re-measuring the node -- ruinous across 117 of them, so `--kf-mx`/`--kf-my` are set directly on the element; its 3D tilt is dropped, because React Flow owns the wrapper's `transform` for positioning; and its `useReducedMotion` gate is dropped, which that repo's own CLAUDE.md forbids in new code. The scheme-specific `--kf-wash-alpha` keeps the accent wash as texture rather than information.
- **Stroke weight is ONE scale, in `--kf-stroke-*` tokens, and four readers depend on it** (the skeleton rule, the base relation rule, the three closeness rules, and the legend's sample lines). Before this they each named their own number and had already drifted: the base relation rule said `1` while `is-close-1` said `0.75`, so an edge's weight depended on whether its closeness class had matched, and 0.75 lost its dash rhythm entirely below about 0.8 zoom -- the channel saying "acquaintance" was destroying the channel saying which KIND. Now faint 1.1 / soft 1.5 / firm 1.9 / skeleton 2.3 / close 3, evenly spaced so the scale is learnable.
- **The closeness scale is centred ON the skeleton weight, and only because relations are revealed.** It used to be capped under 2.3 at every step so an always-on relation could never outweigh a parentage. A revealed relation is not competing with the skeleton -- it is the answer to a question just asked, and the hierarchy is established by the resting canvas -- so it now runs soft 1.5 / skeleton 2.3 / close 3. Restore the always-on overlay and this inverts back into a lie.
- **The accent is spent on revealed relations, and reclaiming it from the skeleton was the second measured defect.** `.is-partner` was `--color-accent-dim`, so **68 of 150 family edges rendered amber** (`rgb(138,101,38)` live) -- nearly half the skeleton wearing the one colour reserved for "you" and "this relation", which is not an accent but a theme. A partnership needs no colour: it is the one family edge running HORIZONTALLY, so its geometry distinguishes it for free at every zoom. The rule survives only to set `stroke-linecap: butt`, which makes the horizontal run meet its drop lines squarely where the skeleton's round cap bulges past the join.
- **Direction is a tapered stroke (`#kf-taper`), never an arrowhead.** `MarkerType.ArrowClosed` was removed for three independent reasons: a 14px triangle on a 400px bezier lands where the line meets a card and reads as part of the card's border; React Flow hoists markers into a shared `<defs>` and writes the colour as an INLINE style, so the head could not inherit dimming, the active state or the past colour (a dimmed edge kept a full-contrast head); and it was redundant with the dash flow, which is legible along the whole line. The gradient must use `gradientUnits="objectBoundingBox"` so ONE def serves paths running in every direction -- `userSpaceOnUse` would need per-edge coordinates. `is-ended` overrides the `url()` reference rather than the colour, because a gradient stroke cannot be recoloured. `var()` DOES resolve inside `<stop stop-color>` (verified on a live element). Both stops are now `--color-accent`, matching the revealed relation's own stroke -- a taper fading to a different hue than the line it belongs to reads as two marks rather than one line arriving.
- **A gradient's faint end has to be contrast-scored on the COMPOSITE, like any other alpha.** The taper once faded far enough to fall below the 3:1 meaningful-graphics floor. `--kf-taper-fade` is therefore scheme-specific and must be remeasured against the current canvas whenever the palette changes. Never fade an edge to 0: the terminus is where a reader looks to see who the relation lands on, and transparency there reads as a cut-off line rather than an arrival.
- **`--color-edge-soft` is content, not decoration.** Kinfolk is a contact graph as much as a pedigree, so a friendship cannot be the faintest mark on the canvas. Both schemes declare and score the token independently; it remains the fallback and legend sample while a revealed relation itself uses the accent. Recheck the computed stroke, not only the source token, after palette or React Flow changes.
- **There is deliberately NO `.is-relation.is-active` rule, and its deletion is load-bearing.** It recoloured a lit relation to the accent, raised its weight, and looped a 700ms dash flow -- all to pick one line out of fifty-two. Now that relations are revealed, `is-active` matches every relation on screen, so the rule would say "this one" about all of them. Worse, its literal `stroke-dasharray: 5 4` overrode the category rhythm (destroying the only channel that separates a mentorship from a friendship, exactly when the viewer asked to see it) and its infinite animation would put every revealed line into perpetual motion -- the "fifty animated edges is a screensaver" failure, arrived at from the other side.
- **Counting an enum into a `Record` needs a fallback even when the column is `notNull`.** `census.ts` is pure and counts whatever it is handed, and `sex[undefined]++` does not throw -- it silently widens the record with an `"undefined"` key holding `NaN`, which the legend then tries to render as a row for a mark that does not exist. Hence `sex[person.primary.sex ?? "unknown"]++`; a test pinned the key count at 4 while the suite existed.
- **Contrast is a measurement, not a taste call.** Text owes 4.5:1 (WCAG SC 1.4.3) and an edge, ring or arrowhead owes 3:1 (SC 1.4.11) against the surface behind it, so anything drawn with `opacity` has to be scored on the COMPOSITE (`fg*a + bg*(1-a)`) rather than on the token. That is why closeness is stroke weight only, an ended relation gets `--color-edge-past` instead of a fade, and the `.kf-dim` rules sit at 0.5 / 0.6 rather than the 0.28 / 0.25 that took a dimmed name to 2.29:1. Prefer a second token over an opacity: a value can be checked, a composite has to be recomputed every time the backdrop moves.
- **Edge colour is semantic and scheme-specific.** `--color-edge` carries the family skeleton; `--color-edge-soft` carries relation fallback and legend samples. The skeleton rule is `.react-flow .react-flow__edge-path`, doubly qualified on purpose: React Flow's stylesheet is imported after globals.css, so a single-class rule loses on source order and silently falls back to the library default. Read the computed stroke from a real element in both themes whenever an override or token changes.
- **The layout stack is elkjs + React Flow, and the alternatives were measured rather than assumed** (2026-07-29, versions live from the npm registry). Do not swap one in without re-running this: `family-chart` (3.5k weekly) and `relatives-tree` (2.1k) both require a binary `gender`/`Gender` on every node, so adopting either means corrupting a record whose `sexEnum` is `["female","male","other","unknown"]` and defaults to `"unknown"` -- and neither models non-family relations, which is half of what Kinfolk is. `d3-flextree` is a strict tree (one parent per node) and last published 2021-11-08; a family tree is a DAG. `d3-dag` 1.2.2 is the one real contender (DAG-native, variable node sizes, 56k weekly) but it is synchronous on the main thread with no edge routing, so it trades ELK's worker and port routing for nothing we lack. `sigma`, `cytoscape`, `g6` and `@cosmograph/cosmos` (still `2.0.0-beta.20`, 2024-12-19) are force-directed network renderers built for thousands of nodes, which is the opposite of a generation-banded pedigree. Already on the latest `elkjs` 0.12.0 and `@xyflow/react` 12.11.2.
- **`react-tree-graph` was evaluated on 2026-07-29 and rejected, on the same grounds as `d3-flextree`.** Registry facts, not memory: 8.0.3, published 2025-01-13, 3.7k weekly, built on `d3-hierarchy` (pinned `^2.0.0`, a full major behind the current 3.1.2 from 2022). `d3-hierarchy` is "layout algorithms for visualizing HIERARCHICAL data" -- a strict tree, one parent per node. A Kinfolk graph is a DAG: a child belongs to two unions (birth and adoptive), parentage hangs off `unions` precisely so remarriages and half-siblings work, and half the product is non-family relations that carry no generation at all. Feeding that to a strict-tree layout means either dropping the second parent or duplicating the node. It also has no edge routing, no port model and no non-family edge concept, so it cannot express the sibling bar, the relation overlay, or `anchorFamilylessNodes()`. The name matches what Kinfolk looked like three commits ago; it does not match what the schema models.
- **Never send real user records, credentials, or private deployment data to an external LLM API.** Synthetic source and fixtures still need the same secret/PII review as any other outbound data.

## Demo mode

Sample data is reachable before auth exists, following the ledger-sync pattern: ONE UI, two data sources, a flag, and mutation guards. The difference is that Kinfolk renders on the server, so the flag is an httpOnly cookie read in a server component rather than a client store -- which also keeps `lib/tree/sample.ts` out of the client bundle entirely.

- Both paths produce the same `TreeView` ([lib/tree/view.ts](lib/tree/view.ts)), so no component knows which one it is rendering.
- [lib/tree/demo.ts](lib/tree/demo.ts) calls the real `fuseTrees` and `toFlowGraph` rather than shipping pre-computed positions. If fusion breaks, the demo must break too; a demo that cannot fail is a screenshot, not a proof.
- Mine-only mode drops the other tree AND its links, so it is a genuinely smaller dataset rather than the same tree with nodes hidden. That is what a viewer without a share grant would actually get.
- A real session beats the demo cookie, so signing in from inside the demo shows your own (possibly empty) tree instead of silently keeping sample data on screen.

## Status

Current version: 0.3.2, 2026-08-08. This release adopts the MIT license and public repository metadata after the owner explicitly accepted the audited historical PII exposure. Runtime behavior remains the profile-first editing and public-release hardening delivered in 0.3.1. See [CHANGELOG.md](CHANGELOG.md).

0.3.2 validation requires Biome formatting/lint, both strict TypeScript projects, both production builds, dependency audits, current-tree privacy checks, and `git diff --check`.

The visualisation layer was rewritten 2026-07-31, keeping the data model and every `lib/tree/` contract intact. What is new: light + dark + system themes on `[data-theme]` with a no-flash bootstrap (inline in Next, external under the SPA CSP) and a fresh contrast-scored token set; an in-app motion switch instead of `prefers-reduced-motion`; a person detail panel (side rail on a pointer device, bottom sheet on a phone) listing parents, partners with their union status, children, siblings, connections read from the subject's own end, channels-without-values and sources; DAG-safe expand/collapse in both directions with per-card hidden counts; a visited-history breadcrumb trail that rewinds rather than repeating; eleven keyboard shortcuts whose bindings and help sheet come from one array; and loading / error / alone / search-empty / offline states. `motion` (now v13) was added and is used for CHROME only -- the 117 cards and 150 edges stay CSS keyframes, because a JS animation re-renders a node and React Flow re-measures on render.

Two defects were found by writing the tests rather than by looking at the screen, and both are recorded in Gotchas: collapse blocking a person instead of their union (which renders a marriage with one participant), and a collapse walk seeded from the roots being unable to fold ancestors at all. A third was found by grepping the built CSS: this file's own gotcha note was compiling a dead Tailwind rule into production.

The connectors were rebuilt from scratch on 2026-07-31 and two more arrangements added, all recorded in Gotchas. Geometry moved to [lib/tree/paths.ts](lib/tree/paths.ts) with 14 tests: hard-corner edges went 82 of 150 -> 3 (the three are straight drops), and the 6013px sibling bar is gone. An ORBIT view landed alongside the pedigree ([lib/tree/radial.ts](lib/tree/radial.ts), 19 tests), centring one person with everybody else on rings by relationship distance -- it took four measured iterations to get right, and the lesson is in Gotchas: the tangle was the ANGLES (122 degree median span, 26 of 34 edges over 90) rather than the curves, and bowing them harder only made loops. Final geometry: 1 degree median span, 0 over 90, max radius 1680px, nodes 19/16/21/19 across the quadrants. A per-card 3D tilt was added and verified to keep hit-testing intact (13 of 14 visible tilted cards clickable at 151 tilted nodes).

Three more were found only by SCREENSHOTTING the canvas on 2026-07-31, after every per-element measurement had passed -- all three are in Gotchas. The living-status colour was painting 80% of every surface green (card rails, 93 of 152 minimap rects, 93 of 117 dots), the fold chevrons were 35px hit targets inside a scaled layer rather than 44, and the mobile detail sheet at 70dvh was covering the very card it described. Verified live afterwards on a clean reload at 1440x900 and 375x812, in both themes: 117 cards, 150 edges, ONE stroke treatment at rest, 0 amber family edges, 0 green rails, 0 relation edges at rest, 148 of 148 fold targets at 44 screen px across three zoom levels, no console errors, and no contact value anywhere in the detail panel (the only phone-shaped match in its text was the lifespan "1985 - 2025").

The canvas rework landed 2026-07-30 and was measured before and after in the browser, not reasoned about: the resting canvas went from 202 edges wearing 15 distinct stroke treatments to 150 edges wearing ONE, the 52 relation edges (73% of all edge ink, median span 2214px) became a per-person reveal, and the 68 amber partner edges went neutral. Confirmed live that clicking a person surfaces exactly their relations in the accent with the label showing, at desktop and 375px.

The schema is live on a real Neon branch as of 2026-07-29, and `loadTreeView()` was verified against it by a live smoke script, since removed (22 live checks: fusion across two graphs, kinship terms, contact filtering at `linked` access, mine-only, the no-graph case, and a pending link changing nothing). New-account provisioning was verified against the same branch: one graph, one self node with `sex` left `unknown`, `rootPersonId` set, a non-null `loadTreeView()` returning kinship "you", idempotent on a second call, and clean teardown. `/api/auth/providers` returns 200 with the callback on port 3007.

The public SPA is live at <https://sagargupta.online/kinfolk/> (Pages, serving the `frontend/` build), and the API plus server-rendered fallback are live at <https://kinfolk-neon.vercel.app>. Changes are logged in [CHANGELOG.md](CHANGELOG.md).

Setup for a fresh clone is [docs/SETUP.md](docs/SETUP.md), which is where the click-by-click Neon and GitHub OAuth steps live -- `.env.example` points at it rather than restating them. Production is [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md): four workflows in [.github/workflows/](.github/workflows) covering CI, database migration plus endpoint verification, daily health checks, and the Pages SPA deployment. Vercel's own Git integration builds the Next app, so `deploy.yml` deliberately only does what Vercel cannot: get the schema ahead of the code and assert afterwards that the deployment answers.

Two deploy rules worth stating outright. **`deploy.yml` runs `db:migrate`, never `db:push`** -- push diffs the live schema and applies what it infers, which can drop a column it thinks is redundant; `migrate` runs the committed SQL and nothing else. And **CI needs no database secret at all**, verified by running the build with `DATABASE_URL` unset: `lib/db/client.ts` is importable without one and nothing queries at build time, so the only env the build gets is a throwaway `AUTH_SECRET` to stop Auth.js logging `MissingSecret`.

**`ci.yml` is inline rather than calling `Sagargupta16/shared-workflows`, and the shared workflow was improved instead of forced to fit.** At the time it lacked three things this repo needed -- a typecheck step, env for the build, and tests BEFORE the build. Rather than fork it, `node-ci.yml` gained `run-typecheck`, `tests-before-build` and a `build-env` secret on 2026-08-05, all defaulting to the old behaviour so the five repos already calling it are untouched (`code-arena`, `Contact-Manager-Mern`, `Financial-Dashboard`, `ledger-sync`, `orbit`). Kinfolk still does not call it: since the 0.2.0 rework its CI is one job -- lint, both typechecks, the Next build, the Vite Pages build, and the dead-Tailwind guard -- and that guard greps THIS repo's built CSS, so it would have to be an inline step anyway. Revisit only if a second repo needs the same guard. That repo runs actionlint in its own CI, which is the check that cannot be run locally here.

The editor is profile-first: select a person and use **Add relative** for parents, partners, siblings, or children. The advanced sheet keeps only relation and partnership tools for connecting people who already exist. Non-structural connections can be removed individually from a profile without deleting either person or changing parentage.

Sign-in works end to end as of 2026-07-29: the GitHub OAuth app exists, a real round trip completed, and sign-out is verified down to the session row being deleted from Postgres. Sharing by invite shipped alongside it (invite by email or GitHub username, 14-day expiry, `viewer` by default, claimed in the `signIn` event) with 10 live checks including viewer-cannot-edit and expired-invite-refused. There is no mail sender, so an invite returns its explanatory text for the inviter to pass on.

Not built yet: merge-proposal UI, contact editor UI (the action exists, the form does not), person DELETE form (same), attaching an existing child to a partnership from the UI. The panel's avatar is initials rather than a photo because `photoKey` has no signed-URL route yet -- rendering an `<img>` would 404 on every card or leak a bucket path.

Person editing shipped 2026-07-31: a pencil in the detail panel header opens a form over the read view, covering name, birth surname, nickname, gender, living status, birth and death years, birthplace, current place, work, notes and a source note. It writes through `updatePerson`, which was rewritten to patch by key presence after the old version was found to null three fields it never rendered. Contact VALUES stay out: the panel still shows only which channels exist, because a canvas gets screenshotted and this component cannot tell a screenshot from a private read.

The canvas was screenshotted for the first time on 2026-07-30, and that is what found the rework. Earlier sessions could only measure per-element (the Browser pane would not composite -- `document.visibilityState === "hidden"` -- so edge paths read 0), and every individual value checked out: each token cleared its contrast floor, each stroke weight sat on the scale. The picture was still wrong, because nothing per-element can see that 52 lines share a canvas. **Screenshot the canvas before claiming a visual change is done** -- a metric per mark cannot answer how the marks add up.
