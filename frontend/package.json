{
	"name": "kinfolk-frontend",
	"version": "0.3.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"preview": "vite preview",
		"typecheck": "tsc --noEmit"
	},
	"//": "Versions are pinned to the root package.json on purpose. The components are shared by alias, so two copies of React or React Flow at different versions would be resolved differently by the two builds -- and a hook mismatch surfaces as an inscrutable runtime error rather than an install failure.",
	"dependencies": {
		"@xyflow/react": "^12.11.2",
		"elkjs": "^0.12.0",
		"lucide-react": "^1.29.0",
		"motion": "^13.0.0",
		"react": "^19.2.8",
		"react-dom": "^19.2.8"
	},
	"devDependencies": {
		"@tailwindcss/vite": "^4.3.3",
		"@types/react": "^19.2.18",
		"@types/react-dom": "^19.2.4",
		"@vitejs/plugin-react": "^6.0.5",
		"tailwindcss": "^4.3.3",
		"typescript": "^7.0.2",
		"vite": "^8.2.1"
	}
}
